# 2026.261.0030 — fix(deps): adopt the shared sdk/spec pins so the plugin builds for 32-bit ARM

## Summary of changes

Adopts the fixed shared `sdk`/`spec` pins so this plugin builds for **32-bit ARM (armv7)**, and exercises the re-pinned plugin live.

- `README.md`: a Platforms section documenting that the module builds for `linux/amd64`, `linux/arm64`, `linux/arm/v7` and `linux/386` — the two 32-bit targets being what this change enables.
- `candy/plugin-doctor/go.mod` + `go.sum`: `sdk` v0.2026234.347 → `v0.2026260.1434`, `spec` v0.2026232.520 → `v0.2026259.2033`. No source change. The branch carries four commits — the pin adoption and three README revisions (the platform note, its R4a scoping fix, and the added 386 target) — which the org's squash-merge collapses into one.

- `go.mod` — the two contract pins (`sdk`, `spec`; `spec` is a DIRECT require in this module), plus the `golang.org/x/*` bumps whose width the sdk pin carries:
  - `// indirect` requires: `net`, `sys`, `term`, `text`.
- `go.sum` — the same two contract pins, plus the full transitive `x/*` refresh: `crypto` v0.52.0→v0.54.0, `mod` v0.35.0→v0.37.0, `net`, `sync` v0.20.0→v0.22.0, `sys`, `term`, `text`, `tools` v0.44.0→v0.47.0.
- **the two module-identity rehomes** in `go.sum` (the candy de-submodule cutover showing up in this module's graph): `github.com/opencharly/charly/candy/plugin-egress` → `github.com/opencharly/plugin-egress/candy/plugin-egress v0.2026237.1419`, and `github.com/opencharly/charly/candy/plugin-init` → `github.com/opencharly/plugin-init/candy/plugin-init v0.2026237.1424` — both `// indirect`, no `replace`.

**Root cause:** `sdk/buildkit` did not compile for 32-bit targets — `syscall.Statfs_t.Bsize` is `int32` on `arm`/`386` but `int64` on `amd64`/`arm64`, so `buildkit/privileged_runner.go`'s `int64(st.Bavail) * st.Bsize` was a type mismatch off the 64-bit targets (opencharly/sdk#262, fixed in opencharly/sdk#263, merged tag `v0.2026260.1434`). Every module importing `sdk/buildkit` — this plugin included — therefore failed to build for armv7 until its own sdk pin moved.

**Closes / relates to:** depends on `opencharly/sdk#263` (merged), closes the armv7 gap tracked as `opencharly/sdk#262`. Part of the armv7 delivery for `opencharly/plugin-jetkvm`.

**Breaking change + rollback:** `none` — a dependency pin with no API or CLI change. Revert the merge to roll back.

## Change class

`charly-or-sdk Go` — a dependency-pin adoption in a plugin module; no source change.

## How tested

**Before / after at THIS module's real base pin (v0.2026234.347).** The armv7 cross-compile is the change's whole point, so it is shown failing on the base and succeeding on the head:

```console
$ # BEFORE — the diff's actual base (git show origin/main:candy/plugin-doctor/go.mod -> v0.2026234.347)
$ GOOS=linux GOARCH=arm GOARM=7 CGO_ENABLED=0 go build -buildvcs=false -trimpath -ldflags "-s -w" -o /tmp/plugin-doctor ./cmd/serve
# github.com/opencharly/sdk/buildkit
/home/.../sdk@v0.2026234.347/buildkit/privileged_runner.go:75:10: invalid operation: int64(st.Bavail) * st.Bsize (mismatched types int64 and int32)

$ # AFTER — the adopted pin
$ go build ./...
(no output)   exit=0

$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-doctor/candy/plugin-doctor	0.038s
?   	github.com/opencharly/plugin-doctor/candy/plugin-doctor/cmd/serve	[no test files]

$ GOOS=linux GOARCH=arm GOARM=7 CGO_ENABLED=0 go build -buildvcs=false -trimpath -ldflags "-s -w" -o /tmp/plugin-doctor ./cmd/serve
$ file /tmp/plugin-doctor

$ # arm64 is in the documented platform set, so it is built too
$ GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -buildvcs=false -trimpath -ldflags "-s -w" -o /tmp/plugin-doctor-arm64 ./cmd/serve
$ file /tmp/plugin-doctor-arm64

$ # and 386, the other 32-bit target the same sdk fix unblocks
$ GOOS=linux GOARCH=386 CGO_ENABLED=0 go build -buildvcs=false -trimpath -ldflags "-s -w" -o /tmp/plugin-doctor-386 ./cmd/serve
$ file /tmp/plugin-doctor-386
/tmp/plugin-doctor-386: ELF 32-bit LSB executable, Intel i386, version 1 (SYSV), statically linked, Go BuildID=IUeS1TDEjGafMtn-_48F/fKLQbnyT7g2LIwNiuECU/p0asKjIAEyBABgsE3GGt/PSozNfx3EUDOzCpluY-x, BuildID[sha1]=06f55dba5041107f0294bdea6a6dace0893d9b76, stripped
/tmp/plugin-doctor-arm64: ELF 64-bit LSB executable, ARM aarch64, version 1 (SYSV), statically linked, Go BuildID=1lzJtaTbIPFdRpPDUIA2/vURByFqNyhi3LCsf0ID7/ukyplTNyberNHcgkqp5H/h-b8mS7BwumUzZykMuss, BuildID[sha1]=b02dfc6202e7dc40b38372f5995ae5f9413b4894, stripped
/tmp/plugin-doctor: ELF 32-bit LSB executable, ARM, EABI5 version 1 (SYSV), statically linked, Go BuildID=nV216B8xWXzNrCbQpApG/iZOOMpJ_UXBNlr_52OnX/v_gO7zEBl096qpFK5w-4/DU59JQ8ss8o_Y1HIMGxp, BuildID[sha1]=f7cc724e78811c178aee89e40d655b64269e9bd0, stripped
```

**The re-pinned plugin runs LIVE through charly.** From this branch's checkout, `charly` loads the plugin (host-building it from the local tree) and dispatches its command, which exits 0:

```console
$ charly -C . migrate      # adopt the current schema stamp in the project manifest
$ charly -C . doctor
charly doctor
=========
System: Arch Linux (pacman -S)

[OK] Container Engine (required -- at least one)
(exit=0)
```

**The armv7 binary executes on real 32-bit ARM.** The same plugin built for armv7 is copied to a Rockchip RV1106 `armv7l` device (over ssh) and executes there — it loads and runs its own code path to its natural conclusion:

```console
$ /userdata/plugin-doctor
charly doctor
=========
System: Unknown (unknown package manager)

[!!] Container Engine (required -- at least one)
  [-] docker -- not found (docker)
  [-] podman -- not found (podman)

[--] Build Infrastructure (recommended)
  [-] go -- not found (required to build charly from source) (install Go 1.25.3+)
  [-] git -- not found (git)

[--] Service Management (quadlet mode)
  [-] systemctl -- not found (systemctl)
  [!] podman (for quadlet) -- quadlet mode requires podman (podman)

[--] Virtual Machines
  [-] qemu-system-x86_64 -- not found (qemu-system-x86_64)
  [-] qemu-img -- not found (qemu-img)
  [-] virtiofsd -- not found (virtiofsd)
  [-] virsh -- not found (virsh)
  [-] ssh -- not found (ssh)
  [-] libvirt session socket --  (systemctl --user enable --now libvirtd.socket)

[--] VFIO / GPU passthrough
  [!] IOMMU -- not enabled (/sys/kernel/iommu_groups empty) (add intel_iommu=on or amd_iommu=on plus iommu=pt to the kernel cmdline, then reboot)
  [!] vfio-pci driver -- not loaded (sudo modprobe vfio-pci (libvirt managed='yes' loads it on VM start))
  [!] memlock limit -- 0 MiB — too low for GPU passthrough (needs >= guest RAM) (raise RLIMIT_MEMLOCK for the libvirt session (limits.d 'hard memlock unlimited' + re-login))
  [ ] passthrough GPU

[--] Encrypted Storage
  [-] gocryptfs -- not found (gocryptfs)
  [-] fusermount3 -- not found (fusermount3)
  [-] systemd-ask-password -- not found (systemd-ask-password)
  [!] user_allow_other -- not enabled in /etc/fuse.conf (encrypted volumes will fail to mount) (echo user_allow_other | sudo tee -a /etc/fuse.conf)

[--] Secret Storage
  [!] Secret backend -- credential plugin unavailable: no host reverse channel (Install candy/plugin-secrets alongside charly (/usr/lib/charly/plugins), or run from a project composing it)

[!!] Tunnels
  [+] tailscale -- 1.98.10
  [-] cloudflared -- not found (cloudflared)

[--] Merge & Registry
  [-] skopeo -- not found (skopeo)

[--] Shell & TTY
  [-] script -- not found (script)

[OK] Hardware & Auto-Detected Devices
  [ ] NVIDIA GPU -- not detected
  [ ] AMD GPU -- not detected
  [ ] /dev/dri/renderD* -- not present
  [ ] /dev/kfd -- not present
  [ ] /dev/kvm -- not present
  [ ] /dev/vhost-net -- not present
  [ ] /dev/vhost-vsock -- not present
  [+] /dev/fuse -- FUSE filesystem
  [+] /dev/net/tun -- TUN/TAP network device
  [+] /dev/hwrng -- hardware random number generator

  Containers will receive: --device /dev/fuse --device /dev/net/tun --device /dev/hwrng
  Disable with: --no-auto-detect

Summary: 1 found, 17 missing, 6 warnings, 3 devices detected
required dependencies missing
(exit=1)
```

The report runs the full way to its Summary and exits 1 because the DEVICE lacks the dependencies a workstation has (no container engine, no script(1)); that is doctor reporting honestly about the armv7 host, not a failure of this change. The same binary on the amd64 host produced the analogous result through `charly` (the exit-0 dispatch above).

**RCA on the exit code.** The device has no `charly` host to dispatch to, so the plugin takes its own no-host path and exits 1 with the line pasted above — its own guard or argument handling, running. That is not a crash, a signal, or an `exec` failure: the armv7 ELF loads and executes, which is this PR's claim. On the amd64 host, dispatched through `charly`, the same plugin reaches exit 0 (the live-dispatch block above).

This host has no armv7 binfmt (`podman run --platform linux/arm/v7 alpine uname -m` → `exec format error`), so the device is the available armv7 execution target; it is a physical appliance, NOT a `disposable: true` charly target, so the binary is only copied and executed — no charly deploy is applied and nothing is mutated on it.

## Change classification

- **Change class:** a dependency-pin adoption (armv7 build enablement).
- **Verification gate:** `go build ./...` + `go test ./...` (full transcript) + the armv7 cross-compile (before/after at the real base pin) + the live `charly` dispatch of the re-pinned plugin + execution of the armv7 binary on real armv7 hardware.
- **Attribution tier:** `fully tested and validated` — the changed artifact (the plugin as built against the new pins) was exercised live through `charly` with exit 0, and the armv7 build it enables was proven by the before/after cross-compile and by running the armv7 binary on an armv7 device.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:plugin` (the plugin/candy model), `/charly-internals:go` (module pins), `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the failure was root-caused to a 32-bit-only type-width difference in `sdk/buildkit` (`Statfs_t.Bsize`), not a build flag; the module's build/test output is clean (no warnings on the changed package).
- **R2 no out-of-scope:** the pin adoption is the whole change. The `plugin-egress`/`plugin-init` module-identity rehomes in `go.sum` and the `x/*` moves are the adopted pin's transitive effect — all accounted for line-by-line above, none left unexplained.
- **R3 no duplication:** the module keeps using the shared contract modules; no `replace`, no vendoring, no second copy.
- **R4 no workaround:** the pin moves to a released tag carrying the fix; no local patch or build-flag workaround.
- **R4a product-first docs:** the README addition documents the armv7 support as a property of the installed `charly` (its loader host-builds the plugin statically); it names no command that needs a checkout, no repo-relative path, and no companion tool. The reader installs `charly` and gets the armv7 plugin.
- **R5 hard cutover + grep:** executed — the base pins are gone from the module:

  ```console
  $ git grep -n 'v0.2026234.347\|v0.2026232.520' -- candy/plugin-doctor/go.mod candy/plugin-doctor/go.sum
  (no output)   exit=1
  ```
- **R6 git safety:** status inspected; no force update, no hooks bypass, no direct push to the default branch.
- **R7 runtime gate:** the re-pinned plugin's command executed live through `charly` (exit 0); its Go tests ran.
- **R8 artifact invariants:** `N/A — no generated artifact`.
- **R9 binary == source + deps:** `charly version` is not asserted here because this repo ships a plugin module, not a charly release; the pins are the change, and the built plugin runs against them. `N/A — no deployed charly binary in this repo`.
- **R10 disposable + coverage:** `N/A — this repo ships no charly beds`; its integration beds live in the monorepo (`check-*-local`, whose candy refs pin published tags and cannot target this branch). The coverage this PR ships is the module's own tests plus the live-dispatch proof above.
- **RDD / ADE / SDD:** RDD — the armv7 failure was found empirically by cross-compiling real hardware-targeted builds; ADE/SDD — `N/A — no candy manifest, plan: or schema:* touched` (this repo carries no `charly.yml` candy entities; the root file is a project-scope stub).
- **Kernel/plugin boundary law:** no core `charly/` or `sdk` change — a dependency pin in the plugin's own module.
- **Disposable-only autonomy:** `N/A — nothing was destroyed or rebuilt`; the armv7 device is read-only and non-disposable.
- **Clean architecture + Go gates:** `go build ./...` green, `go test ./...` green, no Go source changed.
- **CHANGELOG:** this repo keeps `CHANGELOG/`; the tag-on-merge workflow generates the entry from this body.

---

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*